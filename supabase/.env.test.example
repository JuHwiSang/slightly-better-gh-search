# Test Environment Variables
# Copy to supabase/.env.test and fill in actual values

# Local Supabase (from `pnpm supabase start`)
# These are used by the TEST CLIENT to manage users and Vault.
# - `Project URL` -> SUPABASE_URL
# - `Authentication Keys - Publishable` -> SUPABASE_ANON_KEY
# - `Authentication Keys - Secret` -> SUPABASE_SERVICE_ROLE_KEY
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=<copy-Publishable-key>
SUPABASE_SERVICE_ROLE_KEY=<copy-Secret-key>

# GitHub PAT (acts as OAuth token for testing)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for code search)
TEST_GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

# Upstash Redis (same as dev)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# CORS (local)
ALLOWED_ORIGINS=http://localhost:5173

# Cache TTL (optional, defaults shown)
CACHE_TTL_CODE_SEARCH_SECONDS=3600
CACHE_TTL_REPOSITORY_SECONDS=86400
