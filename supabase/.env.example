# Supabase Edge Functions Environment Variables
# This file is for LOCAL DEVELOPMENT ONLY
# Use this with: supabase functions serve --env-file supabase/.env
#
# Note: SUPABASE_URL and SUPABASE_ANON_KEY are automatically provided
# by Supabase runtime and do NOT need to be set here.

# Supabase Service Role Key (required for Vault access)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# CORS Configuration (required)
# Comma-separated list of allowed origins
# Example for local dev: http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# Upstash Redis Configuration (required for caching)
UPSTASH_REDIS_REST_URL=your-redis-url
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Cache TTL (optional, default: 86400 seconds = 24 hours)
CACHE_TTL_SECONDS=86400
