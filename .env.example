# SvelteKit Environment Variables
# Copy this file to .env.local and fill in your values
# These variables are used by the SvelteKit application (frontend + SSR)

# Supabase Configuration (required)
PUBLIC_SUPABASE_URL=your-supabase-url
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ============================================================================
# Edge Functions Environment Variables
# ============================================================================
# Edge Functions do NOT read from .env file automatically.
# You must set these using Supabase CLI:
#
# Local development:
#   supabase functions serve --env-file supabase/.env
#
# Production deployment:
#   supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your-key
#   supabase secrets set ALLOWED_ORIGINS=https://your-app.vercel.app
#   supabase secrets set UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
#   supabase secrets set UPSTASH_REDIS_REST_TOKEN=your-token
#
# See supabase/.env.example for Edge Functions configuration template.
